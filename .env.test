# LalaSearch Test Environment Configuration
# This file configures isolated test resources to avoid interfering with development data

# === Agent Configuration ===
AGENT_MODE=all

# === Database Configuration (Apache Cassandra) ===
# Use test keyspace for isolated testing
CASSANDRA_HOSTS=127.0.0.1:9042
CASSANDRA_KEYSPACE=lalasearch_test

# === Search Engine Configuration (Meilisearch) ===
# Use test index for isolated testing
MEILISEARCH_HOST=127.0.0.1:7700
MEILISEARCH_INDEX=documents_test

# === Crawler Configuration ===
QUEUE_POLL_INTERVAL_SECS=5
USER_AGENT=LalaSearchBot/0.1 (E2E-Test)

# === Runtime Configuration ===
RUST_LOG=info

# === S3 Storage Configuration ===
S3_ENDPOINT=http://127.0.0.1:9000
S3_REGION=us-east-1
S3_BUCKET=lalasearch-content
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin
S3_COMPRESS_CONTENT=true
S3_COMPRESS_MIN_SIZE=1024
